doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title #{pageTitle} | CheckInn

    //- CSS NỘI TUYẾN - Rất quan trọng cho email
    //- Trong production, hãy dùng tool để inline các style này vào từng element
    style.
      /* Reset */
      body, table, td, a { -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
      table, td { mso-table-lspace: 0pt; mso-table-rspace: 0pt; }
      img { -ms-interpolation-mode: bicubic; border: 0; height: auto; line-height: 100%; outline: none; text-decoration: none; }
      body { height: 100% !important; margin: 0 !important; padding: 0 !important; width: 100% !important; font-family: Arial, 'Helvetica Neue', Helvetica, sans-serif; }

      /* Variables */
      :root {
        --brand-blue: #007bff;
        --text-color: #333333;
        --text-light: #777777;
        --bg-light: #f8f9fa;
        --border-color: #e9ecef;
      }

      /* Container */
      .container {
        width: 100%;
        max-width: 600px;
        margin: 0 auto;
        border-collapse: collapse;
      }

      /* Header */
      .header {
        background-color: #ffffff;
        padding: 20px 30px;
        border-bottom: 1px solid var(--border-color);
      }
      .logo {
        font-size: 28px;
        font-weight: 700;
        color: #007bff;
        text-decoration: none;
      }

      /* Content */
      .content {
        background-color: #ffffff;
        padding: 30px;
      }
      .content h1 {
        font-size: 24px;
        font-weight: 600;
        color: #007bff;
        margin-top: 0;
        margin-bottom: 15px;
      }
      .content p {
        font-size: 16px;
        line-height: 1.6;
        color: var(--text-color);
        margin-bottom: 20px;
      }
      .content p.lead {
        font-size: 18px;
        font-weight: 500;
        margin-bottom: 25px;
      }
      .content .secondary-text {
        font-size: 14px;
        color: var(--text-light);
      }

      /* Button (CTA) */
      .button-container {
        padding: 10px 0 20px 0;
      }
      .button {
        background-color: #007bff;
        border-radius: 5px;
        color: #ffffff;
        display: inline-block;
        font-size: 16px;
        font-weight: bold;
        padding: 14px 28px;
        text-align: center;
        text-decoration: none;
      }

      /* Info Box */
      .info-box {
        width: 100%;
        background-color: var(--bg-light);
        border: 1px solid var(--border-color);
        border-radius: 5px;
        margin-bottom: 25px;
      }
      .info-box td {
        padding: 15px;
        font-size: 16px;
        color: var(--text-color);
      }
      .info-box .info-label {
        color: var(--text-light);
      }
      .info-box .info-data {
        font-weight: 600;
        text-align: right;
      }
      .info-box-divider {
        border-top: 1px solid var(--border-color);
      }

      /* Footer */
      .footer {
        background-color: var(--bg-light);
        padding: 30px;
        text-align: center;
      }
      .footer p {
        font-size: 13px;
        color: var(--text-light);
        margin-bottom: 10px;
      }
      .footer a {
        color: #007bff;
        text-decoration: none;
      }

      /* Mobile responsive */
      @media (max-width: 600px) {
        .container {
          width: 100% !important;
        }
        .content {
          padding: 20px !important;
        }
        .footer {
          padding: 20px !important;
        }
      }

  body(bgcolor="#f0f4f8")
    table(border="0" cellpadding="0" cellspacing="0" width="100%")
      tr
        td(bgcolor="#f0f4f8" align="center" style="padding: 20px 0;")
          //- Bắt đầu Container
          table.container(border="0" cellpadding="0" cellspacing="0")
            //- Header (Logo)
            tr
              td.header(align="left")
                a.logo(href=logoLink style="font-size: 28px; font-weight: 700; color: #007bff; text-decoration: none;") CheckInn

            //- Content Block (Nội dung email sẽ vào đây)
            tr
              td.content
                block content

            //- Footer
            tr
              td.footer
                p(style="font-size: 13px; color: #777777; margin-bottom: 10px;") Đội ngũ CheckInn hỗ trợ bạn 24/7.
                p(style="font-size: 13px; color: #777777; margin-bottom: 10px;") Vui lòng liên hệ 
                  a(href="mailto:support@checkinn.vn" style="color: #007bff; text-decoration: none;") support@checkinn.vn
                  |  nếu bạn cần giúp đỡ.
                p(style="font-size: 13px; color: #777777; margin-bottom: 10px;") © #{new Date().getFullYear()} CheckInn. Mọi quyền được bảo lưu.
                p(style="font-size: 13px; color: #777777; margin-bottom: 10px;") #{companyAddress}

          //- Kết thúc Container
